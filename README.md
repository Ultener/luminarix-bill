# 🚀 Luminarix – Инструкция по развёртыванию на VDS

**Luminarix** – это современная панель управления для хостинга игровых серверов на базе Pterodactyl.  
Данное руководство поможет вам установить и настроить Luminarix на собственном виртуальном сервере (VDS).

---

## 📋 Требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| ОС | Ubuntu 20.04+ / Debian 11+ | Ubuntu 22.04 LTS |
| RAM | 512 МБ | 1 ГБ+ |
| CPU | 1 vCPU | 2 vCPU |
| Диск | 5 ГБ | 10 ГБ |
| Node.js | 18.x | 20.x LTS |
| Доступ | root или sudo | root |

---

## 📖 Содержание

1. [Подготовка сервера](#1-подготовка-сервера)
2. [Установка Node.js](#2-установка-nodejs)
3. [Загрузка проекта](#3-загрузка-проекта)
4. [Автоматический деплой (рекомендуется)](#4-автоматический-деплой-рекомендуется)
5. [Ручная установка](#5-ручная-установка)  
   5.1. [Установка зависимостей и сборка](#51-установка-зависимостей-и-сборка)  
   5.2. [Настройка Pterodactyl API](#52-настройка-pterodactyl-api)  
   5.3. [Запуск сервера](#53-запуск-сервера)  
   5.4. [Настройка автозапуска (systemd)](#54-настройка-автозапуска-systemd)  
   5.5. [Настройка Nginx (домен + SSL)](#55-настройка-nginx-домен--ssl)  
   5.6. [Настройка домена](#56-настройка-домена)
6. [Проверка работы](#6-проверка-работы)
7. [Обновление проекта](#7-обновление-проекта)
8. [Решение проблем](#8-решение-проблем)
9. [Структура проекта](#9-структура-проекта)
10. [Данные админ-аккаунта](#10-данные-админ-аккаунта)
11. [Безопасность](#11-безопасность)
12. [Быстрый старт (TL;DR)](#12-быстрый-старт-tldr)

---

## 1. Подготовка сервера

### Подключение к серверу
```bash
ssh root@ВАШ_IP_АДРЕС
```

### Обновление системы
```bash
apt update && apt upgrade -y
```

### Установка необходимых пакетов
```bash
apt install -y curl wget git nano ufw
```

### Настройка фаервола (рекомендуется)
```bash
ufw allow 22       # SSH
ufw allow 80       # HTTP
ufw allow 443      # HTTPS
ufw allow 3000     # Node.js (временно, для теста)
ufw enable
```

---

## 2. Установка Node.js

### Вариант 1: Через NodeSource (рекомендуется)
```bash
# Установка Node.js 20.x LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Проверка версии
node -v   # Должно показать v20.x.x
npm -v    # Должно показать 10.x.x
```

### Вариант 2: Через NVM
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
```

---

## 3. Загрузка проекта

### Вариант А: Через Git (если проект в репозитории)
```bash
cd /opt
git clone https://github.com/ваш-репозиторий/luminarix.git
cd luminarix
```

### Вариант Б: Загрузка файлов через SCP (с локального компьютера)
```bash
# На ВАШЕМ КОМПЬЮТЕРЕ выполните:
scp -r /путь/к/проекту root@ВАШ_IP:/opt/luminarix
```

### Вариант В: Загрузка через SFTP (FileZilla / WinSCP)
1. Подключитесь к серверу через SFTP (порт 22)
2. Загрузите всю папку проекта в `/opt/luminarix`

### Вариант Г: Создание проекта вручную на сервере
```bash
mkdir -p /opt/luminarix
cd /opt/luminarix

# Создайте все файлы проекта вручную через nano/vim
# или загрузите их любым удобным способом
```

> **Важно:** все дальнейшие шаги предполагают, что файлы проекта находятся в каталоге `/opt/luminarix/`.


---

## 4. Автоматический деплой (рекомендуется)

Если вы хотите максимально автоматизировать установку, используйте готовый скрипт развёртывания.  
Скрипт самостоятельно выполнит все необходимые шаги: установку зависимостей, сборку проекта, настройку systemd, Nginx и SSL (при наличии домена).

### 🔥 Быстрая установка одной командой

Самый простой способ установки — запустить скрипт напрямую из репозитория:

```bash
bash <(curl -s https://raw.githubusercontent.com/Ultener/luminarix-bill/refs/heads/main/install.sh)
```

### 📦 Локальная установка

Если вы уже скачали репозиторий, скрипт находится по пути:

```
/opt/luminarix/deploy/install.sh
```

**Порядок действий:**

1. **Сделайте скрипт исполняемым:**
   ```bash
   chmod +x /opt/luminarix/deploy/install.sh
   ```

2. **Запустите скрипт:**
   ```bash
   bash /opt/luminarix/deploy/install.sh
   ```

3. **Следуйте интерактивным подсказкам** (если они предусмотрены):
   - Введите домен (или оставьте пустым, если используете только IP)
   - Укажите API‑ключ Pterodactyl (если скрипт запросит)
   - Подтвердите установку SSL (при наличии домена)

### Что делает скрипт:
- ✅ Проверяет наличие Node.js и устанавливает его при необходимости
- ✅ Устанавливает зависимости (`npm install`)
- ✅ Выполняет сборку фронтенда (`npm run build`)
- ✅ Создаёт systemd‑сервис и включает автозапуск
- ✅ Устанавливает и настраивает Nginx (как reverse proxy)
- ✅ При наличии домена получает SSL‑сертификат через Let's Encrypt
- ✅ Настраивает фаервол (открывает порты 80, 443)
- ✅ Запускает сервис и проверяет работоспособность

### Меню возможностей:

При запуске скрипта вы увидите меню:

```
0. Установить панель — полная установка с нуля
1. Удалить панель — полное удаление всех компонентов
2. Обновить панель — обновление до последней версии
3. Выйти — завершить работу скрипта
```

### После завершения работы скрипта:
- 🚀 Сервер будет запущен как systemd‑сервис `luminarix`
- 🌐 Nginx будет настроен и, при указании домена, выпущен SSL‑сертификат
- 📍 Проект будет доступен по домену или IP‑адресу

> **💡 Важно:** если вы используете метод `bash <(curl -s ...)`, вам **не нужно** отдельно скачивать репозиторий — скрипт сделает это автоматически! Все действия выполняются в интерактивном режиме с понятными подсказками.

> **📌 Примечание:** при использовании автоматического скрипта вы можете **пропустить разделы 5–5.6** – скрипт выполнит все эти действия автоматически. Для проверки переходите сразу к разделу [6. Проверка работы](#6-проверка-работы).

---

### 📋 Пример работы:

```bash
# Просто запускаем команду
bash <(curl -s https://raw.githubusercontent.com/Ultener/luminarix-bill/refs/heads/main/install.sh)

# Появится меню:
╔════════════════════════════════════════╗
║     Luminarix Bill Manager v1.0       ║
╚════════════════════════════════════════╝

Статус: ○ не установлена

0. Установить панель
1. Удалить панель
2. Обновить панель
3. Выйти

Выберите действие [0-3]:
```

---

Такой текст:
- ✅ Понятно объясняет оба способа установки
- ✅ Выделяет самую простую команду жирным шрифтом
- ✅ Показывает меню и возможности
- ✅ Сохраняет все важные детали о работе скрипта
- ✅ Использует эмодзи для наглядности
---

## 5. Ручная установка

Если вы предпочитаете выполнить все шаги вручную или хотите лучше понять процесс, следуйте инструкциям ниже.

### 5.1. Установка зависимостей и сборка

```bash
cd /opt/luminarix

# Установка зависимостей
npm install

# Сборка фронтенда (создаст папку dist/)
npm run build

# Проверка что dist/ создалась
ls -la dist/
# Должен быть файл index.html
```

### 5.2. Настройка Pterodactyl API

Откройте файл `server.js` и укажите ваши данные:
```bash
nano /opt/luminarix/server.js
```

Найдите и измените:
```javascript
const PTERO_URL = 'https://console.luminarix.fun';  // URL вашей Pterodactyl панели
const PTERO_ADMIN_KEY = 'ptla_ВАШ_КЛЮЧ';           // Application API Key
```

**Как получить API ключ Pterodactyl:**
1. Войдите в **Pterodactyl Admin Panel**
2. Перейдите в **Application API** (правый верхний угол → Application API)
3. Нажмите **Create New**
4. Описание: `Luminarix Billing`
5. Все права установите на **Read & Write**
6. Скопируйте сгенерированный ключ (начинается с `ptla_`)

### 5.3. Запуск сервера

**Тестовый запуск (в терминале):**
```bash
cd /opt/luminarix
node server.js
```

Вы должны увидеть:
```
═══════════════════════════════════════════
🚀 Luminarix Server запущен!
📍 http://localhost:3000
📡 Pterodactyl: https://console.luminarix.fun
🔑 API Key: ptla_...
═══════════════════════════════════════════
```

Остановите тестовый запуск нажатием `Ctrl + C`.

### 5.4. Настройка автозапуска (systemd)

Создайте файл сервиса:
```bash
nano /etc/systemd/system/luminarix.service
```

Вставьте:
```ini
[Unit]
Description=Luminarix Hosting Panel
Documentation=https://github.com/luminarix
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/luminarix
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=5
Environment=NODE_ENV=production
Environment=PORT=3000
LimitNOFILE=65536
StandardOutput=journal
StandardError=journal
SyslogIdentifier=luminarix

[Install]
WantedBy=multi-user.target
```

Активируйте и запустите:
```bash
systemctl daemon-reload
systemctl enable luminarix
systemctl start luminarix
systemctl status luminarix
```

### 5.5. Настройка Nginx (домен + SSL)

Установите Nginx:
```bash
apt install -y nginx
```

Создайте конфигурацию:
```bash
nano /etc/nginx/sites-available/luminarix
```

Базовая конфигурация (замените `my.luminarix.fun` на ваш домен):
```nginx
server {
    listen 80;
    server_name my.luminarix.fun;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
```

Активируйте сайт:
```bash
ln -s /etc/nginx/sites-available/luminarix /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl restart nginx
```

**Установка SSL (Let's Encrypt):**
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d my.luminarix.fun
```

### 5.6. Настройка домена

В DNS-панели вашего домена создайте A-запись, указывающую на IP вашего VDS.

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | my | IP_ВАШЕГО_VDS | 3600 |

Проверьте DNS:
```bash
dig my.luminarix.fun
```

---

## 6. Проверка работы

### Чек-лист:
```bash
# Статус сервиса
systemctl status luminarix

# Порт 3000 слушается
ss -tlnp | grep 3000

# Локальный тест API
curl http://localhost:3000/api/ptero/test

# Тест через домен
curl https://my.luminarix.fun/api/ptero/test
```

### В браузере:
1. Откройте `https://my.luminarix.fun`
2. Должна открыться главная страница Luminarix
3. Войдите с админ-данными (см. раздел [10. Данные админ-аккаунта](#10-данные-админ-аккаунта))
4. Перейдите в "Админ панель" → "Pterodactyl" и нажмите "Тестировать" – должно показать "Подключено"

---

## 7. Обновление проекта

### Если используется Git:
```bash
cd /opt/luminarix
git pull
npm install
npm run build
systemctl restart luminarix
```

### Если файлы загружаются вручную:
1. Замените файлы в `/opt/luminarix`
2. Выполните:
   ```bash
   cd /opt/luminarix
   npm install
   npm run build
   systemctl restart luminarix
   ```

---

## 8. Решение проблем

### ❌ Сервер не запускается
```bash
journalctl -u luminarix -n 50
cd /opt/luminarix && node server.js  # ручной запуск для отладки
```

### ❌ Pterodactyl API не работает
```bash
curl -H "Authorization: Bearer ptla_ВАШ_КЛЮЧ" https://console.luminarix.fun/api/application/servers
```

### ❌ Ошибка "EADDRINUSE" (порт занят)
```bash
lsof -i :3000
kill -9 PID
```

### ❌ 502 Bad Gateway (Nginx)
```bash
systemctl status luminarix
cat /var/log/nginx/error.log | tail -20
```

### ❌ SSL не обновляется
```bash
certbot renew --force-renewal
systemctl restart nginx
```

---

## 9. Структура проекта

```
/opt/luminarix/
├── server.js           # Бэкенд-сервер (Express.js)
├── package.json        # Зависимости
├── vite.config.ts      # Конфигурация сборки
├── index.html          # HTML-шаблон
├── tsconfig.json       # Конфигурация TypeScript
├── src/                # Исходный код фронтенда
│   ├── App.tsx
│   ├── main.tsx
│   ├── store.ts
│   ├── index.css
│   └── pages/
│       ├── Landing.tsx
│       ├── Auth.tsx
│       ├── DashboardLayout.tsx
│       ├── MyServers.tsx
│       ├── ServerManage.tsx
│       ├── PurchaseFlow.tsx
│       ├── AccountSettings.tsx
│       ├── TopUp.tsx
│       └── AdminPanel.tsx
├── dist/               # Собранный фронтенд (после npm run build)
├── deploy/             # Скрипты деплоя
│   └── install.sh      # Автоматический установщик
└── node_modules/       # Зависимости
```

---

## 10. Данные админ-аккаунта

| Поле | Значение |
|------|----------|
| **Username** | `kailyteam_mishakakawka` |
| **Email** | `mishakakawka123@mail.ru` |
| **Пароль** | `KailyTeam2026ope` |
| **Баланс** | 99,999₽ |

> ⚠️ **Рекомендуется сменить пароль после первого входа!**

---

## 11. Безопасность

- Измените пароль админ-аккаунта
- Закройте порт 3000 после настройки Nginx:  
  ```bash
  ufw delete allow 3000
  ```
- Регулярно обновляйте Node.js и зависимости
- Делайте резервные копии пользовательских данных (localStorage хранится в браузере каждого пользователя – подумайте о внешней БД для продакшена)

---

## 12. Быстрый старт (TL;DR)

```bash
# 1. Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 2. Загрузка проекта (пример через git)
cd /opt
git clone https://github.com/ваш-репозиторий/luminarix.git
cd luminarix

# 3. Автоматический деплой
chmod +x deploy/install.sh
bash deploy/install.sh

# 4. Готово! Откройте https://ваш-домен или http://IP
```

---

*Ultener development © 2026. Все права защищены.*
